[package]
name = "ohagent-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core agent loop, config, and provider bridge for ohAgent"

[dependencies]
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }

# Jcode integration — path deps into git submodule
jcode-app-core = { path = "../../jcode/crates/jcode-app-core" }
jcode-provider-core = { path = "../../jcode/crates/jcode-provider-core" }
jcode-agent-runtime = { path = "../../jcode/crates/jcode-agent-runtime" }
